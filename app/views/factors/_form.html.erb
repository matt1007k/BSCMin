<%= form_with(model: factor, local: true) do |form| %>
  <% if factor.errors.any? %>
    <div class="message is-danger">
      <h2 class="title is-5 has-text-danger"><%= pluralize(factor.errors.count, "error") %> al realizar la operación:</h2>
      <div class="message-body">
        <ul>
          <% factor.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%= form.hidden_field :force_id, value: force_id %>
  <div class="field">
    <%= form.label :title, class: "label is-normal" %>
    <div class="control"> 
      <%= form.text_area :title, class: "textarea is-info" %>
    </div>
  </div>

  <div class="field is-grouped margin-small-topons">
    <%= form.submit "Guardar", class: "button is-primary" %>
  </div>
<% end %>

